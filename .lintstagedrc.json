{
  "*.{ts, tsx}": ["bash -c tsc --pretty --noEmit"],
  "*.{js,ts,tsx}": [
    "npx eslint --ignore-path .eslintignore --fix .",
    "prettier --ignore-path .gitignore --write"
  ],
  "*.{json}": ["prettier --ignore-path .gitignore --write"]
}
